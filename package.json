{
  "name": "sms-account-rotator",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema local para envio de SMS com rotacao controlada entre contas autorizadas.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check src/config.js && node --check src/accountRotator.js && node --check src/jobStore.js && node --check src/smsProvider.js"
  },
  "dependencies": {
    "smpp": "^0.5.0"
  },
  "engines": {
    "node": ">=18"
  }
}
